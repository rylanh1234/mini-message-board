<html>
    <body>
        <h1><%= title%></h1>
        <ul>
            <% messages.map((message) => { %>
                <li>
                    <h3><%= message.user ? message.user : "Anonymous" %> - <%= message.added%></h6>
                    <p><%= message.text %></p>
                </li>
            <% }) %>
        </ul>
        <a href="/new">Create Message</a>
    </body>
</html>