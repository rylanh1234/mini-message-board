<html>
    <body>
        <h1><%= title%></h1>
        <ul>
            <% messages.map((message) => { %>
                <li>
                    <h3><%= message.user%> - <%= message.added%></h6>
                    <p><%= message.text %></p>
                </li>
            <% }) %>
        </ul>
    </body>
</html>